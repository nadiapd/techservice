{{!-- <div class="mx-auto max-w-3xl space-y-6">
  <div class="rounded-3xl bg-white p-8 shadow-sm">
    <h1 class="text-3xl font-semibold">Hasil Tracking</h1>
    <p class="mt-2 text-slate-600">Detail status service untuk kode <strong>{{service.tracking_code}}</strong>.</p>

    <div class="mt-6 grid gap-6 md:grid-cols-2">
      <div class="rounded-2xl bg-slate-50 p-5">
        <p class="text-slate-500">Nama Customer</p>
        <p class="mt-2 text-lg font-medium">{{service.customer.name}}</p>
        <p class="text-slate-600">{{service.customer.email}}</p>
        <p class="text-slate-600">{{service.customer.phone}}</p>
      </div>
      <div class="rounded-2xl bg-slate-50 p-5">
        <p class="text-slate-500">Status Service</p>
        <p class="mt-2 text-lg font-medium capitalize">{{service.status}}</p>
        <p class="text-slate-600">Masuk: {{formatDate service.entry_date}}</p>
        <p class="text-slate-600">Selesai: {{formatDate service.finished_date}}</p>
      </div>
    </div>

    <div class="rounded-2xl bg-slate-50 p-5">
      <h2 class="font-semibold">Detail Perangkat</h2>
      <p class="mt-3 text-slate-700">{{service.device_name}} / {{service.device_brand}}</p>
      <p class="mt-2 text-slate-600">Kategori: {{service.category.category_name}}</p>
      <p class="mt-3 text-slate-600">Keluhan: {{service.complaint}}</p>
    </div>
  </div>

  <div class="rounded-3xl bg-white p-8 shadow-sm">
    <h2 class="text-2xl font-semibold">Histori Progress</h2>
    <div class="mt-4 space-y-4">
      {{#each service.service_logs}}
        <div class="rounded-2xl bg-slate-50 p-5">
          <div class="flex items-center justify-between gap-4">
            <span class="font-semibold capitalize">{{status}}</span>
            <span class="text-sm text-slate-500">{{formatDate created_at}}</span>
          </div>
          <p class="mt-3 text-slate-600">{{note}}</p>
        </div>
      {{else}}
        <p class="text-slate-600">Belum ada histori update.</p>
      {{/each}}
    </div>
  </div>
</div> --}}

<div class="max-w-3xl mx-auto bg-white rounded-2xl shadow p-8">

  <h1 class="text-3xl font-bold mb-8">
    Service Detail
  </h1>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

    <div>
      <p class="text-slate-500 text-sm">
        Tracking Code
      </p>

      <h2 class="font-semibold">
        {{service.tracking_code}}
      </h2>
    </div>

    <div>
      <p class="text-slate-500 text-sm">
        Status
      </p>

      <h2 class="font-semibold">
        {{service.status}}
      </h2>
    </div>

    <div>
      <p class="text-slate-500 text-sm">
        Customer Name
      </p>

      <h2 class="font-semibold">
        {{service.customer_name}}
      </h2>
    </div>

    <div>
      <p class="text-slate-500 text-sm">
        Device
      </p>

      <h2 class="font-semibold">
        {{service.device_name}}
      </h2>
    </div>

  </div>

  <div class="mt-8">

    <p class="text-slate-500 text-sm mb-2">
      Complaint
    </p>

    <div class="bg-slate-100 rounded-xl p-4">
      {{service.complaint}}
    </div>

  </div>

</div>